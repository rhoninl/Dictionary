# Cloudflare Pages configuration
[build]
command = "npm run build"

[build.environment]
NODE_VERSION = "22"

[site]
bucket = "./dist"
include = ["**/*"]
exclude = ["functions/**"]

# Function configuration
[functions]
directory = "functions"